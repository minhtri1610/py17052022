pip install jupyter
pip install pandas
pip install seaborn
pip install numpy
pip install matplotlib
pip install -U scikit-learn
pip install keras
pip install --ignore-installed --upgrade tensorflow

pip3 install jupyter
pip3 install pandas
pip3 install seaborn
pip3 install numpy
pip3 install matplotlib

ssh-keygen -t ed25519 -C "epsminhtri@gmail.com"
testgit01

pip install dash